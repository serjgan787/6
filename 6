# Your personal GitHub API access token
access_token = 'YOUR_ACCESS_TOKEN'

# Create a Github instance
g = Github(access_token)

# Repository information
repo_name = 'my-repo'
repo_description = 'This is my repository created via PyGitHub'
repo_private = False  # Set to True if you want to create a private repository

# Get the authenticated user
user = g.get_user()

# Create the repository
repo = user.create_repo(repo_name, description=repo_description, private=repo_private)

print(f"Repository '{repo_name}' successfully created!")
print(f"URL: {repo.html_url}")
